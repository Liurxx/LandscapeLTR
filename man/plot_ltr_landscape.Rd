% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_ltr_landscape.R
\name{plot_ltr_landscape}
\alias{plot_ltr_landscape}
\title{Plot LTR Retrotransposon Distribution Landscape}
\usage{
plot_ltr_landscape(
  data,
  target_region = NULL,
  min_width_mb = 0.04,
  track_height = 0.85,
  lane_buffer = 0.005,
  output_file = NULL,
  width = 12,
  height = 7,
  dpi = 300,
  title = "Landscape of LTR Retrotransposons (Centered Axis)",
  return_plot = TRUE
)
}
\arguments{
\item{data}{A data frame or path to a TSV file containing LTR data.
Required columns: TE (format: "Chr:Start-End"), Clade, Strand}

\item{target_region}{Numeric vector of length 2, specifying the genomic
region to plot in Mb (default: NULL, will use data range)}

\item{min_width_mb}{Numeric, minimum width for elements in Mb (default: 0.04)}

\item{track_height}{Numeric, height of each track (default: 0.85)}

\item{lane_buffer}{Numeric, spacing between lanes (default: 0.005)}

\item{output_file}{Character, path to save the plot. If NULL, plot is
returned without saving (default: NULL)}

\item{width}{Numeric, plot width in inches (default: 12)}

\item{height}{Numeric, plot height in inches (default: 7)}

\item{dpi}{Numeric, resolution for PNG output (default: 300)}

\item{title}{Character, plot title (default: "Landscape of LTR Retrotransposons (Centered Axis)")}

\item{return_plot}{Logical, whether to return the ggplot object (default: TRUE)}
}
\value{
A ggplot object if return_plot is TRUE, otherwise NULL
}
\description{
This function creates a visualization of LTR retrotransposon distribution
along a genomic region with a centered axis layout. Clades are balanced
above and below the central axis for optimal visualization.
}
\examples{
\dontrun{
# Using example data
data(ltr_example)
plot_ltr_landscape(ltr_example, target_region = c(66.5, 67.5))

# From file
plot_ltr_landscape("path/to/data.tsv", target_region = c(66.5, 67.5))
}

}
